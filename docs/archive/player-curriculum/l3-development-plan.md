> 보관일: 2026-09-24. 작성 당시의 계획·판정·승인 이력이며 현재 실행 지시가 아닙니다. 현행 기준: [현재 상태](../../soccer/current-status.md).

# L3 전진 패스·연계 개발 계획 — L2 승인 후 실행

현재 상태: L3-A 구현, 공통 Builder 자산 생성, Python41/41, Unity EditMode135/135, Soccer PlayMode18/18, Windows 학습 Player 빌드까지 완료했다. 첫32workers100k 한정 Run과 고정 평가·회귀가 남았다.

## 승인과 출발점

2026-09-05 L2 계열 r018200,184가 정식 검증을 통과했다. L2 최종 최근5요약은 모두 성공50% 이상이었고, 최종 후보의 무보조·난이도1·gate0 고정평가는 seed64213 300경기97%와 seed29471 100경기98%, L0 300경기97.33%, L1 phase2 300경기71%/방향슛78%였다. 원본 L1r017과 모든 L2 Run은 보존한다. L2의 지원위치·Strong 패스조준·수신추격 코드 기술과 개입량을 그대로 명시하며, 성공은 여전히 명시적 유리한 킥·실제2m·현재 확정 수신자를 요구한다. 이 증거로 L3-A 구현을 시작한다.

## 구현 순서

1. 현재 Trainer/Player/Unity/예약 실행을 확인한다. 기존 원본과 승인L1r017은 보존한다.
2. L3-A를 먼저 만든다. Stadium 복사와 별도 L3 lesson/profile을 공통 Builder에서 연결한다. 기존 L2 판정을 재사용하되 **유리한 명시적 인계1회 → 수신 후 전진**의 짧은 과제로 시작한다. 처음부터3인연계·득점·수비를 동시에 요구하지 않는다.
3. 보상은 첫 패스의 부분성과/수신 후 전진 중심으로 격리한다. 수치·거리·상한은 구현 전에 보상표·공통엔진·관련 테스트에 동시에 반영한다. 반복터치·앞뒤왕복·발신자 재소유는 성공연계로 세지 않는다. 추후 슛은 L1복습 맥락에서 별도 레슨으로 다룬다.
4. 모든 Red 선수가 움직이는 조건으로 수신대기 보조를 단계적으로 제거한다. 임시코드보조가 있으면 조건과 개입빈도를 별도기록한다. 관측379/Action[3,3,3,3], 골키퍼보호, 공통물리를 유지한다.
5. Compile/전체Soccer EditMode·PlayMode/Builder Validate/Profile검증/WindowsBuild/ProjectSettings검사를 통과한 뒤 승인된 L2 모델에서32workers·첫100k·50k저장으로 한정학습한다. r007은 미승인이다. 원본Run을 `--force`로 재사용하지 않는다.
6. 독립seed 고정평가에서 패스·수신·전진을 각각 확인한다. L3 자체의 기준은 L2 승인 후 별도로 확정하며 폐기된 근접예외를 재사용하지 않는다. 결과에 따라 같은Run의 제한연장 또는 원인수정을 선택한다.
7. L3-A가 가능한 수준에 이르면 L3-B에서 서로 다른3명의 두번연계, 그 다음 L3-C에서 제한수비를 추가한다. 각단계마다 저장·평가 후 다음요구를 켠다.

## L1 기술 유지

r007의 구평가 L1 성공62.67%/방향슛70.67%는 회귀 약화 신호다. L2 최종승인 전에 회귀를 검증하며 기존 L1 기준도 낮추지 않는다. 원래r017은 계속보존한다. v2 회귀로 확인하고 공통계약을 바꾸지 않는 복습/혼합 방안을 설계한다.

## 자동 실행 범위

현재 상태 문서의 정확한 인계를 먼저 읽는다. 무기한 대기예약은 금지하며 유한학습을 직접감시한다. 제한으로 막히면 실제사용량과 초기화시각을 조회해 가능한시점 이후 단발재개로 조정한다. 오류·중단 시 checkpoint/ONNX/config/log/SHA 및 다음명령을 남긴다. 이번 문서는 L3 구현계획이지 빌드·학습완료 보고가 아니다.
