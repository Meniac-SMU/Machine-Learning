# MS3-v3 worker 기록 충돌 수정 및 재출발

2026-09-25 사용자 승인: 문제를 해결한 뒤 학습을 다시 시작한다. 기존600k 목표와 주간 잔여량4% 이하 저장·중단 조건을 유지한다. 실패 Run r001은 재개하지 않고 승인 MS2 actor에서 새 Run `MNG_MS3V3-20260925-r002`를 시작했다.

## 수정

- spawns/common-rules 기록을 worker와 process ID가 포함된 파일로 분리했다. 평가 reader는 원래 기록과 새 형식을 모두 읽으며 한 경기 디렉터리에 다중 stream이 있으면 거부한다.
- Player 로그의 예외를1초 간격(TrainerController.advance 진행 중)에 증분 검사한다. 발견 시 STOP와 player-failure.json을 남기고 기존 KeyboardInterrupt 정상 저장 경로로 모델·optimizer·pool을 저장한다. trainer가 응답하지 않는 완전 hang까지 해결하는 감시로 주장하지 않는다.
- STOP가 있는 Run의 재개를 거부하며, 격리/Player 실패 Run은 평가 입력으로 거부한다.
- 경기 규칙·보상·관측244·행동6·PPO 설정은 바꾸지 않았다. 환경 revision 의미는 유지하고 새 runtime DLL hash로 수정 빌드를 구분한다.

## 검증

| 검사 | 결과 |
|---|---|
| Python |41/41 |
| Unity EditMode |367/367,32 writer 동시 기록 회귀 포함 |
| Soccer/Manager PlayMode |54통과/0실패/수동진단2제외 |
| 함께 실행된 Legacy Escape |3실패: 보관된 EscapePrototype 씬이 Build Settings에 없음. Soccer 통과와 분리하여 원본 XML 보존 |
| 새 학습·평가 Player |빌드 및 생성/사용 기록 등록 완료 |
|32환경 공용 폴더 스트레스 |2400 step/worker,800마다 명시 Reset,총153472결정,121경기 종료,worker당 최소3경기 |
| 기록 무결성 |32 boot/spawn/common-rule stream,공통 규칙2150기록,JSON 정상,Player 예외0 |
| 동결 D4 재검증 |80경기 무결성 통과,기존 D4의 경기별 결과 전체 동일 |
| 오류 저장 통합검증 |별도 r900에서 의도적 예외를 주입,7552 step에서 모델/optimizer/pool 저장 및 재개 차단;실제 Player 오류0 |
| ProjectSettings |테스트가 추가한 SENTIS define 및 줄바꿈을 시작 시 SHA와 정확히 일치하도록 복구 |

새 빌드는 `Builds/MNG_V2/MS3-v3-WorkerIOFix-20260924T182752604Z/{MS3V2,EvaluationV2}`. runtime SHA는 `8c7c54abba8e36a1ced4d5ec144bf836c36bee484f8f9b67827d776a466b3d9d`다.

동결 기준선은 MS2동일정책45%,Full68.75%,Recover56.25%,carry-shot25%,Balanced75%,무작위 대Full0%로 기존과 같다. 이 결과는80경기의 결과 동일성을 보이며 진영 동등성/학습 개선/모델 승격의 증명은 아니다.

## 실행

최종 준비물: `Logs/MNG-Rebuild/MS3-v3-IO-ready-20260925`. 초기 actor는 기존 승인 MS2 step200120의 actor-only 사본을 step0으로 초기화했다. 새 optimizer와 초기 MS2 pinned 하나의 새 pool을 사용한다. 준비 상한600k,첫100k 저장·정지,100k마다 점검,200k마다 동일300초 상세 평가를 유지한다.

r001과 의도적 오류 검증 r900은 모델/로그를 보존하고 격리한다. r002만 본 학습이며 optimizer/pool을 이어받지 않았다. 현재 실행 진행은 [현재 상태](../current-status.md)를 따른다.

## 증거

`Logs/MNG-Rebuild/MS3-v3-IO-fix-20260925/`: source-manifest.json,repair-diff.patch,edit.xml,play.xml,play-scope.json,python-tests-final.log,d4-analysis.json,d4-equivalence.json,guard-integration-result.json,final-readiness-receipt.json,settings-restored.json.

`Logs/MNG-Rebuild/MS3-v3-IO-preflight32-20260925/report.json`은32환경 검증이다. `MS3-v3-IO-D4-*-20260925`에80경기 raw와 report를 보존했다. 실패/수정 전 준비물 및 모델은 삭제하지 않았다. commit/push는 하지 않았다.

## 이번 실행의 저장 결과

r002는101546 step에서 첫100k를 정상 저장하고100k 모니터링을 통과했다.32 worker/147경기 종료 기록,Player 예외0,optimizer step1131,pinned1/recent10 snapshot을 확인했다.200경기 고정 평가와 전체 trace 검사를 통과했다.

| 상대 | 승/무/패 | 승점률 |
|---|---|---|
| MS2 |17/4/19 |47.5% |
| Full |26/6/8 |72.5% |
| Recover |24/5/11 |66.25% |
| carry-shot |13/11/16 |46.25% |
| Balanced |36/3/1 |93.75% |

Pass 선택9회 중 실제 타격 연결1회로 희소성이 남는다.64회의 집계 intended reception에는 다른 출처 패스도 포함되므로9선택의 성공률로 해석하지 않는다. 상대별 진영 차이도 추적 대상이다. 비소유 상태 비운반 선수의 공 뒤 평균 인원은 상대별 약1.79~1.95명으로 관찰됐으며, 위치 통계이지 감독 명령의 인과 효과나 수비 안전 보장은 아니다. 기존 D4와 seed/표본 수가 다르므로 승점률 차이만으로 개선을 확정하지 않았다.

같은 r002의 optimizer/pool을101546 step에서 복원해200k 구간을 재개했다. 주간 잔여량4% 도달로 정상 저장을 요청했고122738 step에서 종료했다.최종 checkpoint/pool step 일치,유한값 통과,Player 예외0,optimizer step1362.실제 learning transition team0=80400/team1=42338이며 교대 구간 중간에 사용량 때문에 정지했다.200k 평가 및600k는 미완료다.

`Logs/MNG-Rebuild/MNG_MS3V3-20260925-r002/monitor-100k.json`과 `audit-100000/summary.json`이100k 근거이며,`usage-stop-receipt.json`에 최종 저장 모델/optimizer/pool SHA를 기록했다. r002는 격리된 실패 Run이 아니며 같은 checkpoint에서 추후 재개할 수 있다. STOP은 사용량 기준 중단을 유지하기 위한 표시다. 현재 실행 프로세스는 없다.
