# MS v3 운영 기준

마지막 검토: 2026-09-26. 현행 코드·schema·registry·D5 보고서를 대조한 운영 진입점이다. 진행 상태는 [현재 상태](../current-status.md) 한 곳에서 관리한다.

## 정책·런타임 계약

- 팀당 감독 Agent 1개, PPO 숫자 관측. 감독은 전진·패스·슛·회수·균형·후방보호의 6명령을 선택하고 코드가 선수의 이동·목표·물리 킥을 실행한다.
- [schema](ms-v3-schema.json): Behavior `MNG_ManagerV2`, 관측244, action branch[6], task/event version2, protocol3. 기존133 + 선수4×27 + 전역3이다. 세대 v3와 ABI 이름 v2는 별개다.
- 현행 revision은 `MNG-MS2-forward-pass-center-20260923`. `Assets/_Soccer/Manager/Runtime/MNG_RuntimeV2.cs`와 schema가 일치해야 한다.
- MNG 정책형·규칙형·fallback은 같은 기술 경로를 사용한다. 사람 입력 소유권을 유지하고 행동 성공·소유를 강제 생성하지 않는다.
- 구 선수 정책379/[3,3,3,3]와 감독244/[6]는 별도 계약이다.

## 현재 경기 규칙

- 자기 골문 앞 걷어내기는 모든 자동 선수 유형에 유지한다. 정체2초/전진0.1m에 따른 강제 패스는 폐지했다.
- 감독 PassBuild 선택 뒤 전방 동료를 향해 준비하며 실제 경로가 막히면 취소한다. 상대 골문 중심24m 이내에서는 PassBuild를 선택하지 않는다.
- 패스 범위5~28m, 경로 여유1.25m, 예측 수신점 중앙 쪽1.5m 편향, 드리블 목표 중앙 쪽3m 편향(중앙선 초과 금지)을 재사용한다.
- 준비/수신 예산은 각2초. 실제 킥·수신과 준비 요청을 별도로 집계한다. 명령 선택만으로 보상하지 않는다.
- MNG 공·선수 solver48/16 및 Reset의 Transform/Rigidbody 동기화는 유지한다. 이 수치를 별도 Core 환경 전체에 확대하지 않는다.
- 세부 근거: [현행 경기 규칙 보고서](ms2-forward-pass-center-report-20260923.md). 과거 설계가 필요할 때만 [개편 런타임 명세 이력](../../archive/manager/ms-v2/ms-rebuild-runtime-contract-20260922.md)을 본다.

## 정책 계보·실행 경계

### 상시 챔피언 교체·지표 평가 기준 (2026-09-26 사용자 지정)

- 새 모델이 현 챔피언과의 직접 대결에서 누적 승수로 앞서면 챔피언을 교체한다. 새 모델이 지면 현 챔피언을 유지한다. 다른 상대의 평균 성적이나 신뢰구간을 추가 승격 문턱으로 적용하지 않는다.
- 직접 대결은 기본40경기(20 seed×양 진영), 경기당300초, 동일 runtime/schema/경기 규칙으로 실행한다. 기본 경기 뒤 양측 승률 차이 `|승A-승B|/경기수`가5%p 이하이면20경기를 한 번 추가한다. 이후에는 차이가5%p 이하라도 누적 승수가 많은 모델이 승자다. 완전 동률만40경기씩 추가하고 동률이 해소되면 판정한다. 새 대결 및 추가 경기는 새 seed를 사용하고 라운드 내부 경기는 병렬 실행한다.
- 상세 평가는400k 간격을 유지하며100k에는 저장·상태 점검만 한다. 승인 MS2·Full·Recover·carry-shot·Balanced·동일 Run 과거 정책과의 평가도 유지한다. 무작위 상대는 제외한다. 이 경기들은 상대별 강약점과 퇴행을 보여주는 지표이며 챔피언 교체 판정과 분리한다.
- 상대별 승·무·패, 승률/승점률, 득실점, 진영별 성적과 불확실성, 패스 선택 가능량·선택 확률·선택→준비/취소→타격→수신, 수비 위험을 기록한다. 이전 모델과의 지표 비교에는 가능한 한 동일 seed·진영 조건을 유지한다.
- 경기 무결성 실패는 정상 승패로 집계하지 않고 원인을 해결한다. 현재 챔피언은2,001,834 step이다. 이 운영 기준 확정은 추가 학습 실행이나 패스 촉진 변경 적용을 뜻하지 않는다.

[v3 registry](ms-v3-model-registry.json)의 승인 MS2 step200120 actor만 출발 기준이다. 초기 actor 사본은 step0, 새 optimizer, 새 상대 풀로 시작한다. MS2를 새로 학습했다는 뜻이 아니다. 기존 MS3-v2 r005 400k는 모든 현행 선택에서 제외하고 보존한다.

최신 사용자 승인 범위는 같은 r002를2M까지 학습하고 평가하는 것이다.32환경·집계 step을 유지하고100k에는 저장·상태 점검만,400k 간격인1.2M·1.6M·2.0M에는 고정300초 상세 평가를 수행한다. 사용자가40k를400k로 정정한 최신 지시를 따른다. 무작위 상대 평가는 제외한다. 치명적인 문제가 발생하거나 주간 사용량 잔여량4% 이하이면 저장·중단한다. 일반적인 성능 정체와 치명적 무결성 실패는 구분한다.2M 이후 연장은 별도 판단한다.

이 승인 범위는2,001,834 step 저장과1.2M·1.6M·2M 총960경기 평가로 완료했다. 학습·평가·자원 모니터를 종료했다. 당시 champion400719를 유지했으며, 이후 별도 승인 토너먼트로2M(2,001,834 step)를 현 champion으로 확정했다. 상세 결과는 [2M 완료 보고서](../../archive/manager/ms-v3/ms3-v3-r002-2m-training-20260925.md)에 보존한다.1.7M 시작 전 포트 충돌은 저장본 무변경과 원래32개 포트 해제를 확인해 같은 조건으로 복구했다.

최신 후속 지시는0k부터400k 간격으로2M까지 직전 승자와 순차 대결하고 챔피언을 선발하는 것이었다. 기본40경기·300초·16개 병렬, 기본 승률 차이5%p 이하 시20경기 추가, 이후 누적 완전 동률에만40경기를 추가하는 사용자 판정 규칙으로 총220경기를 완료했다. 승자는2M(2,001,834 step)이며 registry에 등록했다. 이전 통계적 승격 조건을 추가 적용하지 않는다. [토너먼트 보고서](../../archive/manager/ms-v3/ms3-v3-r002-tournament-20260926.md)와 원본 completion.json을 따른다. 추가 학습은 수행하지 않았다.

이전200k 재개에만 적용했던 사용량 조건 면제는 종료됐다.200k 결과는 [평가 보고서](ms3-v3-r002-200k-evaluation-20260925.md)에 보존한다. 원본 maximumSteps600000 manifest는 유지하고 별도2M 확장 gate로 재개한다. learning_rate/beta/epsilon schedule은 모두 constant이며 optimizer·pool·학습 규칙은 이어받는다. 명목 최대 step과 과거 모델 보존을 위한 keep_checkpoints21→64만 바꾼다. 진영 동등성·정책 품질은 실제 평가로 판단한다.

상대 풀은 MS2 초기 snapshot 하나를 pinned로 시작하고 새 v3 Run의 snapshot을 쌓는다. 외부 history·구 pool·구400k를 넣지 않는다. [D4·D5 보고서](ms3-v3-d4-d5-report-20260923.md)에 준비물·평가 상대·seed·검증 경계를 고정했다.

## 도구 선택

[도구 색인](../../../Tools/README.md)에서 현행 진입점을 선택한다. 평가 `mng_v3_evaluate.py`, 시연 `watch_mng_v3.py`, 계보 검사 `mng_v3_policy_guard.py`를 사용한다. 이들이 호출하는 v2 구현 모듈과 `MNG_V2_Train.ps1`은 현행 의존성이므로 폐기 대상이 아니다.

준비 패키지는 `Logs/MNG-Rebuild/MS3-v3-D5-prepared-20260923`다. 실행 전에 manifest가 참조하는 코드·build·actor hash를 확인한다. 실제 학습은 해당 작업의 사용자 지시 범위에서만 실행하며 이번 파일 정리는 학습 실행 요청이 아니다.

2026-09-25 r001은100k 무결성 실패로 격리했다. 이후 worker별 기록 분리와 Player 예외 저장/중단을 검증하고 새 r002를 시작했다. 현행 준비물은 `Logs/MNG-Rebuild/MS3-v3-IO-ready-20260925`다. [IO 수정 보고서](ms3-v3-io-fix-20260925.md)와 [현재 상태](../current-status.md)를 따른다. 원래 준비물과 실패 기록은 보존한다.

평가 상대는 승인 MS2·Full·Recover·carry-shot·Balanced·동일 Run 과거 정책이다. 과거100k 평가와400k에서 이미 끝난 무작위 기준선40경기는 이력으로 보존하고 반복하지 않는다. R6 완료 및 첫 champion400719의 독립80경기 판정은 현재 상태와 `r6-completion-gate.json`에 기록한다. 최신 checkpoint와 champion은 별개다.

1M까지의 실행 증거는 `Logs/MNG-Rebuild/MS3-v3-r002-to800k-20260925`에 보존한다.2M 확장 증거는 `Logs/MNG-Rebuild/MS3-v3-r002-to2m-20260925`의 `extension-gate-2m.json`과 `progress-repair-gate-2m.json`으로 연결한다.658256 step에서 Windows 진행 파일 교체 접근 충돌로 저장 후 종료한 문제는 bounded retry와 비치명적 상태 기록 누락 처리로 수정했다. 실제 Windows 파일 잠금 재현을 포함한3개 테스트를 통과했다. 원본 manifest·변경 전후 entry/launcher hash와1M까지의 확장 이력은 보존한다. PPO adapter·게임 빌드·optimizer·pool은 바꾸지 않는다.
