# 문서·에셋 정리 보고서 — 2026-09-24

상태: 정리 적용 완료, 최종 정적 검증 결과는 아래 증거 파일을 따른다. 작업 범위는 `C:\GitHub\Machine-Learning` 내부다. 게임 코드·정책·보상·학습 조건을 변경하지 않았다.

## 정리 결과

- 대체된 기획·완료 보고서 42개를 `docs/archive/manager/{m-stage,ms-v1,ms-v2}`와 `docs/archive/player-curriculum`으로 이동했다. 현행 D4·D5 보고서와 최신 패스·드리블 보고서는 활성 문서로 유지했다.
- AGENTS, current-status, MNG_README의 과거 누적 본문은 Archive에 보존하고 현재 지침·상태만 남겼다. 이전 Curriculum 및 보상 실험 본문도 보존했다.
- 현행 기준은 `ms-v3-current.md`로 모으고 docs/README와 Tools/README에서 작업별 최소 읽기 경로를 제공한다.
- MNG Markdown의 기존 `.meta` 6개와 타이머/메타 64개를 함께 보존했다. 총112파일 이동이며, 비Markdown 70개는 내용 hash를 그대로 유지했다.
- 타이머는 `Assets/_Legacy/Diagnostics/SoccerRuntimeTimers~`와 `MLAgentsTimers-20260924~`로 옮겨 Unity import와 기본 탐색에서 제외했다.
- `Tools/Find-ProjectContext.ps1`을 추가했다. 기본 현행 문서22파일, Manager/Core/Tools 범위 검색, 명시적인 Archive/Legacy 조회를 지원한다. 기본값은 대량 로그·결과·빌드·캐시를 탐색하지 않는다.

## 읽기 분량 변화

UTF-8 파일 크기 기준이며 실제 모델 토큰 측정값은 아니다.

| 진입 문서 | 이전 bytes | 이후 bytes |
|---|---:|---:|
| AGENTS.md | 29,670 | 5,011 |
| docs/soccer/current-status.md | 285,364 | 3,516 |
| Assets/_Soccer/Manager/MNG_README.md | 12,145 | 1,670 |
| docs/README.md | 5,046 | 2,452 |
| 합계 | 332,225 | 12,649 |

네 진입 문서의 본문 크기를 약96.2% 줄였다. 상세 정보는 삭제하지 않고 필요할 때 찾는 보관 자료로 분리했다. 새 운영 기준과 도구 색인은 해당 작업에서만 추가로 읽는다.

## 최신화한 표기

- 현행 세대는 MS3-v3이며 학습 전 D5 준비 완료, 새 R6 학습 미시작이다. 구 v2 R6 완료를 새 세대 실적으로 표시하지 않는다.
- 정체 시간에 따른 강제 패스는 폐지되었다. 감독 선택 PassBuild·자기 골문 앞 걷어내기·중앙 편향 드리블이 현행 규칙이다.
- Core 선수 정책379/[3,3,3,3]과 MNG 감독244/[6]의 적용 범위를 명시했다.
- 기존 Curriculum의 L2/L3 미구현·구 단계 재개 문구를 현재 보존 상태로 정정하고 L2-Find r017 최종 미승인 종료를 README에 기록했다.
- 문서 이동에 따라 활성·보관 Markdown의 상대 링크와 현재 경로 표기를 갱신했다. 이동한 문서 경로를 사용하는 실행 코드 참조는 검색에서 발견되지 않아 런타임 코드를 바꾸지 않았다.

## 보수적으로 유지한 파일

캐시 제외 인벤토리83,320파일을 확인했다. Assets1,854파일 외에 Builds27,461파일/약18,103MiB, Logs50,020파일/약8,944MiB, results3,785파일/약5,388MiB가 있었다. 큰 생성물은 기본 검색에서 제외하되 증거·재현성 때문에 보존했다.

- v3 도구가 v2 이름의 Python 모듈과 실행기를 직접 사용하고 D5 manifest가 실행 소스를 hash로 동결한다. Tools의 물리 위치를 유지하고 용도 색인으로 구분했다.
- MNG Builder는 이전 EvaluationModels 경로를 참조한다. 모델 이름에 diag/옛 날짜가 있다는 이유로 삭제하지 않았다. ONNX별 직접 GUID/문자열 참조 조사도 증거로 보관했으며 무참조를 미사용 확정으로 해석하지 않았다.
- 현재 학습 actor·ONNX, 구400k, r017, 원본 registry·manifest·평가 로그·빌드의 계보를 보존했다.
- `Assets/Hayq Art` 등 외부 에셋과 이전 Scene/Prefab은 사용되지 않음이 확정되지 않아 유지했다. 구 도구·빌드 삭제는0건이다.
- `.gitignore`, Packages, ProjectSettings 및 기존 미커밋 게임 변경을 그대로 유지했다. Git commit/push는 하지 않았다.

## 검증과 증거

정적 검사에서 시작 시점1,804개 소스/설정 파일을 대조했다. 이동 명세에 없는 누락0, 비Markdown 내용 변경0, 이동 비Markdown70개 SHA 일치, import 대상 GUID 중복0·고아 meta0이다. 최종 Markdown 파일 경로 링크 누락0·이전 활성 경로 잔존0을 확인했다. 결과는 `validation.json`을 따른다.

D5 준비 소스97개 및 registry의 초기 actor/ONNX SHA가 일치했다. `launch-first-100k.ps1 -ValidateOnly`는 validated=true/generation=v3/trainingStarted=false로 통과했다. 현행 검색 범위에 archive/Logs/results/Builds/Library가 포함되지 않음과 검색 결과0건 처리를 확인했다.

이번에는 실행 자산/C# 변경이 없어 Unity import·빌드·PlayMode·경기·학습을 실행하지 않았다. 정적 경로/GUID 검사는 Unity 화면 및 런타임 동작 검증과 다르다. 준비 검증은 학습 품질 검증이 아니다.

| 증거 | 위치 |
|---|---|
| 전체 이동 명세·이동 전 SHA | [cleanup-moves-20260924.json](../archive/history/cleanup-moves-20260924.json) |
| 인벤토리·기존 git 상태 | `Logs/ProjectCleanup-20260924/inventory-before.json`, `git-status-before.txt` |
| 소스 기준선·원문 백업 | `Logs/ProjectCleanup-20260924/source-before.json`, `originals/` |
| 정적 검증·분량 비교 | `Logs/ProjectCleanup-20260924/validation.json` |
| 모델·빌드 보존 조사 | `Logs/ProjectCleanup-20260924/asset-retention-review.json` |
| 수행 스크립트 | `Logs/ProjectCleanup-20260924/{organize,finish-docs,validate}.cjs` |

`organize.cjs`와 `finish-docs.cjs`는 이 작업의 일회성 실행 기록이며 재실행하지 않는다. 검증 재확인은 프로젝트 루트에서 `node Logs/ProjectCleanup-20260924/validate.cjs`로 한다. Logs는 기존 Git ignore 대상이므로 전달용 이동 명세와 본 보고서는 docs에 별도 보관했다.

## 다음 작업의 인계

작업 시작은 docs/README → current-status → 필요한 운영 계약 순서다. 아카이브를 기본 Context로 복원하지 않는다. 학습을 요청받으면 원래 D5 manifest와 실행 범위를 확인하고 첫100k 점검 경계를 유지한다. 이번 정리를 이유로 source hash·과거 manifest를 다시 쓰거나 기존400k를 복원하지 않는다. 다른 정리를 계속할 때도 증거 삭제를 자동으로 수행하지 않는다.

완료 인계: [후속 AI 안내](cleanup-handoff-20260924.md). 저장소의 기존 줄바꿈 설정으로 git diff --check도 통과했다.
