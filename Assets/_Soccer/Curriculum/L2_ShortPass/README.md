# L2 - Short Pass

## 현재 구현 단계

확정carrier 진단을 추가했다. 첫 킥 전 최초 실제 소유자가 유지되는 FixedUpdate만 대상으로 지정수신자가3m 미만/16m 초과/골문거리이득2m 미만인지 구분한다. `Confirmed Preparation Samples`가 분모이며 소유소실 표본은 별도다. `Preparation Opportunity Seen/Lost`는 라운드별 사건이고 `First Lost Opportunity ...`는 처음 모든 유리한 대상이 사라진 순간의 조건부 표본이다. 기존 near-ball proxy와 혼동하지 않는다. 진단은 보상/정책/물리/성공을 바꾸지 않으며 새 Player 검증·평가 여부는 현재상태를 따른다.

**2026-09-05 최신 사용자 지시:** L2를 현재 결과로 완료 처리하지 않는다. **패스 성공률50% 이상 필수**이며30% 진입/28% 근접예외는 철회됐다. r007의 v2/패스방향gate0/수신대기1/서로 다른seed 각100경기 성공36%·22%=합산29%는 미통과다. 최근5개 요약 각각50% 이상, 난이도1/양쪽보조0의 독립 고정평가300경기 이상 및 L0/L1회귀를 유지한다. L2 승인 전 L3 개발·학습은 보류한다. `Tools/assess_soccer_l3_entry.py`의 예외승인 경로는 제거했고 기존 provisional JSON은 철회된 이력으로만 보존한다. 명시적 패스/유리한동료/2m/현재확정인계/추가유지0초와 보상·물리는 변경하지 않는다.

r007100k의 두seed 고정 평가에서 gate0 평균26%는 초기와 같고 gate1 평균26%는 초기21.5%보다 소폭 높지만 일관되지 않았다. L0 회귀99.33%는 유지됐으나 L1 성공62.67%/방향슛70.67%로 기준 미달이다. 자동 연장/버전 증가 대신 동일Player L1 기준선 및 근접 준비 행동을 감사한다. 평가 도구의 `--preparation-diagnostics`는 팀소유·plate준비·공1.8m로 고른 근접 proxy의 mask/대상/행동을 기록한다. Vector43에는 carrier identity가 없으므로 실제 소유자 증거로 쓰지 않으며 보상·행동·승급에는 관여하지 않는다. 최신 실제 결과는 현재 상태를 따른다.

현재 `curriculum_l2_pass_gate_poca.yaml`은 r006100,060에서 초기화하는 r007100k 한정 학습 진단이다(16workers/50k 저장). 고정 비교에서 gate만 켜서는 성공이 늘지 않았다(두seed32→27%,20→16%). 다만 첫 실제 킥60/60이 적합한 통로였으므로 신경망이 이동·회전으로 이 기회를 더 만드는지 한 번만 학습 검증한다. 자동 연장은 없다. 동일seed 초기27%/16% 대비 개선과 gate0 전이를 확인해야 하며 최종 승급이 아니다.

2026-09-05 r006 고정100경기 인계24%로 초기24%에서 개선이 없어 추가 연장하지 않았다. 다음 준비 실험은 `soccer_l2_pass_aim_gate=1`(기본0): 확정 Neural carrier가 유리한 동료 통로로 찰 수 있을 때만 킥 branch를 허용한다. 자동 조준/킥/소유 강제는 없고 기존 보상액/성공 기준은 유지한다. 대기 보조와 별개이며 두 보조 모두 난이도1에서는 해제한다. 평가 도구의 `--pass-aim-gate 1`로 고정 모델 비교 후 실제 효과가 있을 때만 다음 학습을 결정한다. 개입 횟수/차단 비율과 보조 없는 평가를 분리한다. 보조 상태나 필수 무보조 통계가 빠진 요약은 최종 승급 불가다.

r005 첫 기회 진단 뒤 준비 배치를 보완했다. 대기 보조의 지정 수신자는14→15.5m에서 시작한다. 최초 확정 소유 때 그 지정 수신자가 유리한 대상이면 고정하고, 그 순간의 예상 킥 방향 품질을 **무보상 기준선**으로 저장한다. 첫 실제 킥 전 동일 최초 carrier가 실제 킥 준비 상태에서 같은 대상 방향의 최고 기록을 높인 증가분만 기존 `CurriculumPassDirection` 라운드 cap0.1 안에서 지급한다. 실제 킥 보상과 cap을 공유하며 대상 교체·기준선 재설정·자동 조준·자동 킥·성공 판정 완화는 없다. 준비 대상/최고 품질/보상 합계·사건 수를 별도로 기록한다. 실제 실행과 비교 평가 상태는 현재 상태 문서를 따른다.

고정 r005200,256 동일 seed 비교에서 첫 실제 킥 대상61.29→75.27%, 통로30.11→35.48%, 인계20→24%로 준비 기회가 개선되어 `curriculum_l2_preparation_poca.yaml`을 단일 가설 r006 최초100k 설정으로 추가했다. r005200,256 초기화,16workers,50k 저장,난이도0/대기1이며 결과를 평가하기 전 자동 연장하지 않는다. 이 비교 상승은 새 정책 학습이 아니라 Player 환경 차이다.

첫 기회 진단은 `First Possession`, `First Kick Ready`, `First Strike`를 분리한다. 각각 최초 확정 제어/최초 FixedUpdate 킥 준비(실제 정책 결정 콜백 아님)/최초 안전 보정되지 않은 Red 명시적 킥에서 대상 존재·방향 통로·거리·시간을1회 기록한다. 방향은 앞 두 시점에는 실제 접촉 혼합식으로 예측한 방향, 첫 킥에서는 실제 방향이다. 대상 없음은 존재 비율0이며 조건부 최선 각도/대상 거리에 가짜0을 넣지 않는다. `First Strike Initial Carrier Sampled`가0이면 소유 샘플 이후 지연/동일 선수 통계는 기록하지 않는다. 보상·행동·난수·성공 판정은 바꾸지 않는다.

직전 방향 실험은 `curriculum_l2_direction_poca.yaml`이다. r005까지는 실제 확정 소유자의 명시적 킥에서 유리한 동료 방향 품질의 최고 기록 증가 보상(cap0.1/팀/라운드)을 주고, 첫 적합한 패스 시도 보상을0.05→0.15로 높였다. 현재 구현은 위 첫 킥 전 고정 대상 개선 구간까지 같은 cap으로 확장했다. 아래0.05 서술은 이전 값이다. 성공 기준/물리/자동 조준 없음은 유지한다. 보상식·변경표는 [보상 기준표](../../../../docs/soccer/rewards.md)를 따른다. buffer40960→10240, batch2048→1024로 정책 데이터 갱신 기회를 늘리되 메모리128/sequence64/POCA/3epochs는 유지한다. 실제 실행/검증 여부는 현재 상태를 확인한다.

L2-A 수신 대기 보조를 구현했다. 기본값은 0이며 준비 YAML에서만 `soccer_l2_waiting_assist=1`로 켠다. 발신자 외 필드 선수들이 대기하고 유리한 실제 패스 뒤에 해제된다. 수신자 초기 위치는 현재15.5m 앞이다. 이것은 **코드가 가르친 임시 준비 행동**이지 학습된 지원 움직임이 아니다. 대기 중 지원 보상은 0이며 보조 시간·조건을 기록한다. Human/Rule/fallback과 일반 경기에는 적용하지 않는다. 난이도 1에서는 보조를 강제로 끄며 최종 모니터에도 무보조 5개 요약을 요구한다. 준비 평가에는 `--waiting-assist 1`, 보조 제거/최종 평가는 기본값 0을 사용한다. 현재 실제 검증·학습 상태는 아래 링크의 최신 인계를 확인한다.

L1 r017 승인 후 L2의 공통 `SoccerCurriculumPassRules`와 Controller의 킥·접촉·수신 타이머를 연결했다. `curriculum-l2` Profile, Builder 전용 생성 함수, 초기화/재개 YAML을 추가했다. 생성·테스트·Player 빌드·학습의 실제 진행 여부는 [현재 상태](../../../../docs/soccer/current-status.md)를 확인한다. 파일이 있다는 것만으로 학습 성공을 의미하지 않는다.

`SoccerProjectBuilder.BuildCurriculumL2Batch`는 기존 L0/L1을 다시 생성하지 않고 L2 Scene/Prefab만 만든다. `curriculum_l2_poca.yaml`은 승인된 L1 모델에서 초기화하며 쉬운 배치 `soccer_l2_spawn_difficulty=0`으로 시작한다. 중단 재개에는 init_path 없는 `curriculum_l2_resume_poca.yaml`과 `--resume`을 사용한다. 난이도 1은 발신 위치 X/Z ±8m, 수신 전방 6~12m와 좌우 ±4m로 확장한다. 에피소드 제한은 20초다.

보상은 최초 소유 0.1, 유리한 패스 시도 0.05, 성공 발신 0.1·수신 0.2, 완료 그룹 0.4·수신자 0.1이다. 각 1회 제한이며 간격 지원은 샘플 0.005/선수별 cap 0.02다. [보상 기준표](../../../../docs/soccer/rewards.md)의 변경표를 따른다. L1 슛/운반 및 모든 일반 전술 보상은 차단한다.

## 목표와 판정

**r003 200k 첫 수신 진단 후 최신 기준:** 킥 시점의 대상 거리3~16m는 유지하되, 실제 공 순변위 요구는3→2m로 완화했다. 공이 수신자 중심에 도달하기 전 접촉하는 현재 물리를 반영한다. 이전 평가16회 수신 중10경기에서 제어는 확인됐으나 3m 이동 조건이 미달이었다. `Required Ball Displacement`로 조건을 기록하며 새 기준 고정 모델 비교 전에는 새 학습 Run을 시작하지 않는다. 아래 3m 이동 계획/결과는 이전 기준이다. 보상 크기·조준·킥·제어 판정과 일반 경기 규칙은 변경하지 않는다.

r003 200k 정체 진단은 `First Reception *`에 적합한 각 패스 비행의 **첫 지정 수신자 접촉** 당시 공 순변위/3m 충족/발신·수신 선수와 공의 거리/비행 시간을 기록한다. `Eligible Strike Target Distance`는 적합한 패스 사건당 거리다. `Interrupted Before/After Reception`은 수신 접촉 전후의 다른 선수 접촉·재킥 횟수(만료 제외), `Confirmed Receiver Short Displacement`는 에피소드 중 현재 수신자 제어는 확인됐으나 순변위가 3m 미만인 적이 있는지다. 진단 통계는 성공 판정·보상·행동을 바꾸지 않는다.

**2026-09-04 r002 진단 후 현재 기준:** L2는 공 인계와 인계 후 유지를 분리한다. 아래 최초 0.75초 계획을 대체하여, 지정 동료가 현재 확정 carrier·최종 접촉자이고 공 2.4m 이내인 것을 FixedUpdate에서 확인하면 추가 유지 시간 없이 인계를 인정한다. 명시적 킥/더 유리한 동료/킥 통로/순변위 3m/8초 제한과 수신 전 재접촉 무효는 유지한다. 단순 터치 콜백이나 우연한 충돌만으로 보상하지 않는다. 일반 경기의 패스 판정은 변경하지 않았다. 기존 0.75초 결과와 새 인계 성공률을 같은 실력 지표로 합치지 않는다. 상세 수치 변경은 보상 기준표의 최신 표를 따른다.

r002 수신 실패 진단은 `Flights Started`, `Receiver/Passer/Other Rekicks`, `Passer/Other Retouches`, `Expired Flights`, `Receiver Confirmed`, `Maximum Receiver Control Seconds`, `Pending At End`를 에피소드별로 기록한다. 기존 `Invalidated Flights`는 재킥을 포함하지 않으므로 새 이유별 통계와 혼동하지 않는다. 계측 자체는 보상·행동·성공 기준을 바꾸지 않는다. 접촉과 확정 소유를 구분한 고정 평가 후 다음 학습을 결정한다.

r001 첫 100k는 적합한 패스 시도 0%로 중단·보존했다. 후속 `Strike *` 통계는 **실제 명시적 킥 접촉 사건**을 분모로 하며, `Diagnostic Target Present At Kick Decision`은 **킥 가능한 결정**을 분모로 한다. 둘 다 에피소드 성공률이 아니고 기존 보상/행동을 바꾸지 않는 원인 분석용이다. 새 Run 전에 두 시점의 타깃·방향·소유 상태를 비교한다.

- Navy 비활성, Red 네 명 Neural. 골키퍼 보호는 그대로 유지한다.
- 처음에는 수신자를 6~8m 앞에 배치하고 발신·수신 필드 선수 역할을 순환한다. 통과 뒤 무작위 배치를 확대한다.
- 유리함은 킥 시점 상대 골문 중심까지의 거리가 발신자보다 최소 2m 짧은 것으로 정의한다. 초보 무수비 L2용 기준이지 수비수·열린 공간을 포함한 일반 전술 최적성 판정은 아니다.
- 대상 거리 3~16m, 실제 킥 방향의 전방 투영 3~16m와 좌우 2.5m 수신 통로를 요구한다. 뒤쪽/옆쪽/지나치게 먼 선수를 향한 임의 킥은 시도 보상 대상이 아니다.
- 성공은 명시적 킥 이후 공 시작점 대비 순변위 3m 이상, 킥 당시 지정된 다른 동료의 연속 0.75초 안정 소유, 공 거리 2.4m 이하, 킥 후 8초 이내다. 원 발신자·제3자의 중간 재접촉은 후보를 무효화한다. 같은 발신자의 최초 킥 플레이트 접촉 콜백만 중복 사건에서 제외한다.
- 일반 엔진의 같은 팀 패스 확인은 0.35초이므로 그것만으로 L2의 0.75초 수신 성공을 대신하면 안 된다. 별도 연속 수신 제어 시간을 검사하고 거리 이탈·접촉자 변경 시 초기화해야 한다.
- 최초 구현에 자동 패스나 조준 코드는 넣지 않는다. L1 슈팅 보조와 운반 전 킥 잠금도 L2에서는 비활성화해야 한다.

## 실행 및 승인 계획

최신2m 거리 기준의 r004는 `curriculum_l2_short_distance_poca.yaml`(r003201,224 초기화/100k 자동 종료)과 대응 Player를 사용한다. 준비 조건은16 workers/대기1/난이도0/50k 저장이다. 근거 있는 연장은 init_path 없는 `curriculum_l2_short_distance_resume_poca.yaml`로 같은 r004 총200k까지만 한다. r003의3m 결과에 현재2m Player를 섞어 재개하지 않는다. 새 기준의 고정13%는 학습 성과가 아니라 실험 기준선이며 L2 최종 승급은 아니다.

r002 진단 후 새 기준의 초기 실험은 `curriculum_l2_handoff_poca.yaml`로 r002 최종 500,380을 초기화한다. 16 workers/50k 저장/**100k 자동 종료** 후 검사한다. 개선 근거가 있을 때만 같은 Run에 `curriculum_l2_handoff_resume_poca.yaml`과 `--resume`을 사용하여 총 500k 이내 연장한다. 초기화 YAML에는 init_path가 있으므로 재개에 사용하지 않는다. 이 과제는 대기 보조가 있는 준비 단계라 최종 L2 승인과 다르다.

L1 `CurriculumL1-20260904-r017/Soccer4v4_Base-99916.pt`를 초기값으로 사용한다. 16 workers, 50k 저장, 첫 100k 진단, 최대 500k 한정 실험이다. 최근 5요약 각각 성공 50%, 소유 85%, 평균 에피소드 15초 이하와 일반 보상 누출 0을 요구한다. 쉬운 배치 통과 뒤 무작위 배치 확대 및 새 seed 300경기, L0/L1 회귀까지 확인해야 승인한다. 상세 계획은 [단계별 보상 계획](../../../../docs/soccer/training/staged-reward-plan.md), 실행 여부와 재개 지점은 [현재 상태](../../../../docs/soccer/current-status.md)를 따른다.
