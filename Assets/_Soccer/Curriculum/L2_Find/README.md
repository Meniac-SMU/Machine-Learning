# L2-Find

> Current status (2026-09-24 review): r017 ended naturally at 500,248 steps and remains unapproved. The final five uniform-360 summaries were 98.61/98.36/97.14/98.59/98.72%, below the strengthened 99% gate. No independent fixed-300 promotion evaluation was run. Preserve PT/ONNX/config/log/SHA evidence in `Logs/CurriculumL2Find-20260907-r017-result`; do not automatically resume or advance to L2-Score/L3. The chronological paragraphs below describe historical experiments, not current execution permission.

Conditional lesson for the approved L2 policy. Four Red agents learn to find a uniformly randomized ball in an empty Stadium. Navy agents are disabled after registration, kicking is masked, and the strengthened episode ends on the first real Red ball contact or after 15 seconds.

Ball centers stay at least 1 m from every field wall and 5 m from the Red goal center. The controller never rotates, steers, or moves a Red agent toward the ball. Reward includes bounded team potential from the nearest Red distance (`0.01/m`, total `-0.5..+0.5`), a bounded signed potential for each of the two actors nearest at spawn, confirmed contact, and lesson completion. Moving away removes the same potential credit, so distance oscillation and recovered distance cannot farm reward.

r002's per-agent high-water reward (`0.005/m`, cap `0.1` each) is preserved as an experiment but is no longer active: same-seed fixed evaluation fell from r001's 75.25% to 45% at 50k and 63% at the final checkpoint. r003 therefore starts again from preserved r001 and changes only the nearest-team shaping to the bounded dense potential above. Code movement assistance remains 0.

r003 reached 82% in the same-seed fixed 100-episode evaluation at 500,272 steps, improving over r001 by 7 percentage points but remaining below the fixed 95% gate. Because the same reward produced measured improvement, the same r003 optimizer may resume once to 1,000,000 total steps. This is not an approval or a relaxed criterion; if the fixed result does not continue improving, revise the learning signal before another run.

The r003 1,000,064 checkpoint reached 82% over an independent fixed 300 episodes. Wide spawns reached 76%, and episodes whose initially nearest actor was a midfielder reached 75%; failed episodes stopped at a mean best distance of 19.04 m. r004 keeps the bounded team potential and adds the same signed bounded potential only to the one actor nearest at spawn. It does not reward all four actors, and it never steers or moves the selected actor. The individual credit can occur every physics update, but its net episode total is always within `-0.2..+0.2` and recovered distance cancels earlier loss rather than farming reward.

r004 completed at 500,656 steps. Its frozen final checkpoint passed two exact 300-episode evaluations with the same independent seed at 288/300 (96%) and 285/300 (95%). The action traces differed, so the evaluations are separate stochastic repeats rather than bitwise replays. Both meet the unrelaxed 95% gate. The approved L2-Find artifact is `Soccer4v4_Base-500656.pt`; L2-Score must initialize from that file without optimizer state. Code movement assistance remained 0 throughout training and evaluation.

The strengthened r005 requirement preserves r004 and starts a new Run from its 500,656 checkpoint. The initial nearest actor receives a signed heading potential of `0.001/degree`, bounded to `-0.1..+0.1`; turning away removes the same credit. A real first contact receives an additional distance-normalized fast-touch reward of at most `0.4`, reaching zero at the 15-second limit. The model already observes the relative ball offset, so no code rotates, steers, accelerates, or moves a player toward it. Peak planar speed, peak closing speed, heading reward, and fast-touch reward are recorded. Code movement assistance remains 0.

r005 completed at 1,000,244 steps and improved the new-seed exact-300 result from r004's 93% baseline to 96.33%, with mean first-touch time improving from 4.127 s to 3.760 s. It still failed the 99% gate. Its 11 failures began at a mean 43.86 m and stopped at a mean best sampled distance of 13.48 m; MiddleThird was 100%, while RedThird, NavyThird, wide spawns, and initially-nearest midfielder episodes contained every failure. The `0.01/m` distance potential was capped at `0.2`, so it saturated after 20 m and often stopped changing before those far episodes reached the ball.

r006 extends the same signed team and selected-actor distance potential cap from `-0.2..+0.2` to `-0.5..+0.5`. The per-meter value, signed recovery, heading potential, fast-touch reward, 15-second terminal rule, and actual-contact success stay unchanged. This preserves a gradient through the last approach for the observed 43.86 m failures without permitting oscillation farming. POCA entropy beta changes from `0.003` to `0.001` to reduce unnecessary random actions near a 96.33% policy; learning rate remains `0.00005`. r006 initializes from the preserved r005 final PT without optimizer state and is limited to 500,000 steps.

r006 completed at 500,216 steps. Its last five summaries were 100%, 98.59%, 96.30%, 94.83%, and 97.10%, and a new-seed exact 300 evaluation was 294/300 (98%). All six failures were Near spawns with mean initial distance 10.74 m and mean best distance 4.39 m; Mid and Far were both 100%. r007 therefore keeps the r006 team potential and changes only the initially selected actor potential: reducing the distance from spawn to a 2 m contact reference is worth at least `0.3`, with adaptive per-meter value capped at `0.05/m` and total still `-0.5..+0.5`. Moving away remains signed and removes prior credit. Actual contact is still the only success, and code movement assistance remains 0. r007 initializes from the preserved r006 final PT at learning rate `0.00003`, beta `0.001`, 32 workers, and is limited to 300,000 steps.

r007 completed at 300,444 steps but its final five summaries were only 92.73% to 96.88%. Fixed 100-episode screens over all six 50k checkpoints peaked at 97% at 149,963 and ended at 94%, so the adaptive near-completion reward is rejected. It increased the value of approaching without the required real contact. r008 restores the r006 fixed `0.01/m` selected-actor potential and adds a group `-0.5` only when the 15-second match timer expires without real Red contact. Successful contact never receives this penalty. The event is emitted before the group episode ends, so the terminal failure signal reaches POCA. r008 starts again from preserved r006, not r007, at learning rate `0.00003`, beta `0.001`, 32 workers, and 300,000 steps. Code movement assistance remains 0.

r008 completed at 300,128 steps. Its last five training summaries were 98.59%, 98.63%, 100%, 98.53%, and 98.46%, so the required five consecutive 99% summaries did not pass. Six fixed 100-episode checkpoint screens were 91/91/95/95/95/98%. A separate new-seed diagnostic 300 reached 298/300 (99.33%), but does not override the failed training-summary gate. The two diagnostic failures approached at high speed and passed the ball without contact. r009 therefore keeps every reward and success rule unchanged and oversamples valid 3-12 m ball starts in 50% of training episodes. Uniform random spawning remains the default and is mandatory for final evaluation. This is a training distribution intervention, with no scripted steering, movement, rotation, or speed.

r009 completed at 300,068 steps. Near success reached 100% in several late summaries, but the final five overall summaries were 97.56/100/98.78/100/100%, so the five-summary gate still failed. Fixed 100 screens reached 100% at 99,988, but a new-seed uniform exact 300 for that checkpoint was only 291/300 (97%): Near 100%, Mid 94.68%, Far 96.92%. The near oversampling therefore traded away uniform Mid/Far generalization and is rejected. r010 returns to preserved r008 final with uniform spawning and keeps all rewards unchanged. It reduces learning rate from `0.00003` to `0.00001` and entropy beta from `0.001` to `0.0003` to stabilize a policy that already reached 298/300 and reduce random actions. r010 is a fresh 32-worker 300k run without optimizer state or code movement assistance.

r010 completed at 300,272 steps, but its final five summaries were 100/95.31/98.57/94.92/98.51%. Lower learning rate and entropy did not stabilize contact, so r010 is rejected without an additional fixed 300. r011 targets the measured failure event rather than changing generic optimizer values: when the minimum distance from any Red player reaches 4 m and later regresses by at least 2 m without actual contact, the Red group receives `-0.2` exactly once. Actual contact remains the only success. A later timeout still receives its separate `-0.5`; the close-miss penalty cannot repeat within the episode. Uniform spawning and code movement assistance 0 remain unchanged. r011 starts from preserved r008 at the prior `0.00003` learning rate and `0.001` entropy, 32 workers, 300k.

r011 completed at 300,172 steps. Its final five summaries were 96.97/100/100/98.61/100%, so the five-summary gate failed. The one-shot close-miss penalty occurred in only 0-1.52% of late episodes and did not prevent near-contact failures from returning after two 100% summaries. r012 rejects that penalty and instead strengthens the required terminal event: a real Red contact receives an additional `+0.6` group success reward, making the total group lesson-success reward `1.0`. Distance/heading shaping, the individual fast-touch reward, timeout `-0.5`, 15-second limit, uniform spawning, and code movement assistance 0 stay unchanged. r012 starts from preserved r008 at learning rate `0.00003`, beta `0.001`, 32 workers, and 300k.

r012 was stopped at 130,448 for the configured usage boundary. Its last five valid summaries were 100/98.46/96.77/96.83/98.31%, so increasing terminal reward did not stabilize success and is rejected. The numbered 130,448 artifacts are preserved; a failed resume later appended r008's 300,128 artifact because the initialization YAML still contained `init_path`, and that bookkeeping contamination is recorded separately. r013 returns to the r008 reward and distribution, holds learning rate at `0.00003`, and changes only entropy beta from `0.001` to `0.0001`. This isolates whether stochastic action pressure causes the remaining rare failures; r010 had changed both learning rate and beta and could not answer that question. r013 initializes from the exact preserved r008 PT without optimizer state, uses 32 workers, and is limited to 300k.

r013 was saved at 71,876 after two resource samples showed 89% RAM use and only 3.42-3.45 GiB available. Its initial 32-worker segment is preserved. The resume YAML removes `init_path` so `--resume` reads the r013 optimizer checkpoint rather than the initialization model; all trainer settings are otherwise identical. The resource policy resumes this same Run with 16 workers. This worker-count change affects throughput only, not the aggregate 300k target or reward/environment contract.

r013 was stopped after its resumed segment reached 127,272. Its last five summaries were 98.31/98.28/97.22/98.31/98.48%; Near was only 91.67% in the last summary. Lower entropy alone is rejected. r014 uses the preserved r008 policy and original beta `0.001`. It rehearses valid 3-12 m starts in 20% of episodes only until 35% training progress, then switches the remaining 65% to the mandatory uniform distribution. This follows r009's evidence that 50% constant near oversampling fixed some Near summaries but damaged Mid/Far uniform evaluation. r014 uses 16 workers due the measured 32-worker memory limit and retains the aggregate 300k target.

r014 started from the preserved r008 policy after its parser, Python, Unity, Builder, Player-build, and ProjectSettings checks passed. A user-requested stop saved the new Run normally at step 656, before its first 10k summary, so it has no performance result and is not approved. Resume the same optimizer and curriculum state only with `curriculum_l2_find_annealed_near_15s_resume_poca.yaml`, `--resume`, the same run ID and seed, as recorded in `Logs/Resume-CurriculumL2Find-20260907-r014.ps1`. Do not reuse the initialization YAML or initialize from r008 again.

r014 later resumed correctly and completed at 300,060. Its final five uniform summaries were 100/96.55/100/96.67/98.59%, while a new-seed uniform 100-episode diagnostic of the final checkpoint was 99%. It is not approved and no fixed approval 300 was run. That diagnostic's only failure had Midfielder as the initially nearest role, while all eight DefenderKeeper-nearest episodes succeeded, so the keeper boundary alone did not explain the failure. The audit did find that only the single initially nearest actor received individual distance and heading credit. L2-Find ends at first real contact, so L1 support spacing and L2/L3 waiting/receiver assistance never run in this lesson. r015 instead preserves timeout `-0.5` and gives the same independent signed distance and heading potentials to the two initially nearest Red actors. This supplies a learned backup pursuer when the keeper is role-constrained without scripting any action, turn, velocity, or position. r015 initializes without optimizer state from the preserved r014 final checkpoint, uses uniform spawning and 16 workers, and is limited to 300k steps. The superseded, untrained timeout-only hypothesis and its first failed validation are preserved separately.

r015 completed at 300,112, but its final five summaries were 95.16/92.00/95.77/90.74/100% and its new-seed uniform 300 result was 291/300 (97%), exactly matching r014. In failed episodes the secondary pursuer's mean best distance worsened from 5.37 m to 10.54 m. Full individual reward duplication is therefore rejected. r016 keeps two learned pursuers because a secondary actor did provide useful backup approach in observed failures, but each receives one half of the former distance and heading potential. Their combined individual shaping budget now equals r014's single-pursuer budget. Team and terminal rewards, uniform spawning, the actual-contact success rule, 15-second timeout, and zero code movement assistance remain unchanged. r016 initializes from r014 rather than the rejected r015 policy.

r016 was stopped after preserving its 252,928 checkpoint because its recent five summaries were 95.83/100/96.77/96.61/98.59%; splitting the reward budget did not remove the rare failures. The r014 fixed 300 audit isolated initial heading as the dominant variable: successful episodes started at a mean 45.65-degree focus heading error, while failures started at 116.72 degrees and were still at 108.29 degrees at the best approach point. The front ray sensor covers +/-60 degrees and the reverse sensor covers 135-225 degrees, leaving two side sectors from 60 to 135 degrees. Vector ball offsets use the world frame, locomotion uses the body frame, and the vector contract has no own-heading feature. r017 therefore keeps the observation size, actions, rewards, two-pursuer split, and actual-contact rule unchanged and trains initial heading limits of 60, 100, 140, then 180 degrees. The final 250k of the 500k run uses the original uniform 360-degree distribution. This is learned search with zero scripted movement, rotation, speed, or contact assistance.

The new approval gate is at least 99% success over an independent fixed 300-episode evaluation, without relaxing the 15-second episode. The previous 95% r004 approval remains preserved historical evidence but does not satisfy the strengthened gate. L2-Score must restart from the strengthened approved checkpoint rather than resume the interrupted r002 initialized from r004.
